[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js（App Router・APIルート）対応のランタイムを明示的に適用
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Netlify の管理画面で Base directory を「keihi-app」に設定してください。
