[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Base directory は Netlify の管理画面で「keihi-app」に設定してください。
# Next.js（App Router・APIルート）対応のランタイムと publish 先は、
# Netlify が package.json を見て自動で適用します（手動で publish を指定しないこと）。
