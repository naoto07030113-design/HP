# セキュリティ方針

## 原則

1. **ログインできることと、すべてを見られることを同義にしない**
2. **画面で隠すことをアクセス制御と呼ばない** — 実際の制御はサーバー側で行う
3. **フロントで検証してもサーバーで必ず再検証する**
4. **迷ったら権限を狭い側に倒す** — 未設定は最小権限として扱う

## 認証

- セッションは Supabase Auth が管理する
- API へは `Authorization: Bearer <access_token>` で渡す（Cookie は使っていない）
- サーバーは毎回 Supabase にトークンを問い合わせて検証する

### ログイン

ログインは `POST /api/v1/auth/login` を通す。以前はブラウザから直接
Supabase を呼んでいたため、失敗が何回続いたかを数えられず、記録も残らなかった。

- 端末（IP）単位で 15分あたり 20回、アカウント（メール）単位で 15分あたり 10回まで
- 成功・失敗とも `audit_logs` に残す（パスワードは渡さない・保存しない）
- 失敗時は「メールアドレスまたはパスワードが正しくありません」だけを返し、
  どちらが違うかは伝えない（アカウントの存在を推測させない）

### 自動ログアウト

受付や施術室の共有端末は開きっぱなしになりやすいため、操作が止まってから
既定30分でセッションを切る（`NEXT_PUBLIC_SESSION_IDLE_MINUTES` で変更可能）。
切れる1分前に画面へ予告を出す。

## 認可

ロールと所属院は **`app_metadata` からのみ** 読む。

`user_metadata` は利用者本人が `auth.updateUser()` で書き換えられる。
以前はそちらを優先して読んでいたため、受付スタッフが自分を管理者に
昇格できる状態だった。現在は `app_metadata` のみを見る。

権限の対応表は `src/server/permissions/policy.ts` が唯一の定義元。

### ユーザーの作り方

Supabase ダッシュボード → Authentication → Users → 対象ユーザー →
**App Metadata** に以下を設定する（User Metadata ではない）。

```json
{ "role": "staff", "clinic_id": "<院のUUID>" }
```

- `role`: `admin` / `staff` / `receptionist`
- `clinic_id`: `admin` 以外は必須。未設定だとデータが表示されない
- 退職者は Supabase 側でユーザーを無効化する

## 患者データの保護

| データ | ブラウザからの直接参照（匿名・ログイン済みとも） |
|---|---|
| 院・スタッフ・メニュー・お知らせ・休診日・物販商品・設定 | 可（公開情報のマスタ） |
| 予約 | **不可**。API が本人確認のうえ本人の分だけ返す |
| 患者情報・カルテ・会計 | **不可** |
| 勤務シフト | **不可**。空き枠は API がサーバー側で算出する |
| 物販予約 | **不可**（患者の氏名・電話番号を含む） |
| 監査ログ・カルテ改訂履歴 | **不可**（管理者が API 経由でのみ参照） |

ログイン済みのスタッフであっても同じ。患者データは必ず `/api/v1/*` を通り、
サーバーが認証・認可・所属院スコープを判定したうえで `service_role` で読み書きする。

患者向けの予約操作は次の API に一本化されている。

| エンドポイント | 本人確認 |
|---|---|
| `POST /api/v1/appointments/availability` | 不要（空き時刻のみ返す） |
| `POST /api/v1/appointments/lookup` | 電話番号 |
| `POST /api/v1/appointments/cancel` | 予約ID＋電話番号 |
| `POST /api/v1/appointments/reschedule` | 予約ID＋電話番号 |
| `POST /api/intake` | 不要（新規予約。枠の空きはサーバーで再確認する） |

`/api/intake` は未認証で誰でも叩けるため、次で守る。

- IP あたり 1時間に 10件までのレート制限
- 院・メニュー・担当者の実在と有効性をサーバーで確認
- 予約枠が本当に空いているかを空き枠算出と同じ経路で再確認
  （画面に空きが出てから送信するまでの間に埋まった場合もここで弾く）
- 予約の状態（status）・患者ID・金額に関わる値は一切受け取らない

「予約IDは存在するが電話番号が違う」場合も `NOT_FOUND` を返し、
IDの存在自体を推測させない。

## 外部への通知

新規予約時のスタッフ向け LINE 通知は、予約が成立したあとに
サーバー内部からのみ送る（`src/server/notifications/lineNotifier.ts`）。

以前は `/api/line/notify` という**未認証のエンドポイント**があり、
誰でも任意の本文を院の LINE 公式アカウントから一斉配信できた。これは廃止した。

通知の失敗で予約は失敗させない（患者の予約は成立させ、失敗はログに残す）。
チャネルアクセストークンはログに出さない。

## ログに残さないもの

`src/server/logging/logger.ts` の `redact` を必ず通す。

- **値ごと削除**: パスワード、アクセストークン、APIキー、Cookie
- **長さのみ記録**: 患者氏名、電話番号、住所、生年月日、カルテ本文、既往歴、服薬、アレルギー

## 秘密情報

- `.env` は絶対にコミットしない（`.gitignore` 済み）
- 環境変数が未設定なら**起動を失敗させる**。本番へ暗黙にフォールバックしない
- `CLINIC_SERVICE_ROLE_KEY` は RLS を迂回する。サーバー側でのみ使い、
  必ず `src/server/http/handler.ts` の認証・認可を通してから到達させる

## RLS（DB 側の遮断）

コード側の移行が終わったので、DB 側でも同じ線を引く
（`supabase/migrations/010_rls_clinic_scope.sql`。**適用はまだ**）。

- 患者データのテーブルにはポリシーを作らない
  = anon / authenticated からは **0 行に見える**
- マスタは参照のみ公開。変更は次のとおり
  - 院・スタッフ・メニュー・お知らせ: 管理者のみ
  - システム設定: 管理者・施術者（コミュニケーション画面から保存するため）
  - 休診日・物販商品: 管理者・施術者、かつ自院のみ
  - 月次レポート: 管理者のみ
- 判定は `auth_role()` / `auth_clinic_id()` / `can_access_clinic()` の3関数に集約する。
  いずれも `app_metadata` のみを見る

適用後の確認: 受付アカウントで `SELECT count(*) FROM patients;` が 0 になること。
管理画面と患者向け予約が今までどおり動くこと。

## 自動テスト

「壊れると業務が壊れる」判断は自動テストで固定している（`tests/`）。
CI で lint / 型チェック / テスト / ビルドを通す（`.github/workflows/ci.yml`）。

- 会計金額の計算式
- 予約枠の重複判定
- ロールごとの権限と所属院スコープ
- 入力スキーマ（合計金額・状態・患者IDを受け取らないこと）
- ログの伏字処理
- レート制限

## 残っている課題

`PROJECT_STATE.md` の「OPEN ISSUES」を参照。とくに以下は未対応。

- 分析レポート・月次レポート・経営会議AI・リマインド・CSV出力が
  ブラウザから Supabase を直接操作している（院をまたいだ制限が効かない）
- RLS に `clinic_id` 条件が入っていない
- レート制限はプロセス内メモリで数えるため、サーバーレスの複数インスタンス間で
  共有されない（本格運用では Redis 等へ差し替える）
