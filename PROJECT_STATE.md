# Project

治療院総合システム（naoto07030113-design/hp）

複数院の鍼灸整骨院向け。予約・患者・電子問診・カルテ・会計・CRM・スタッフ・シフト・
経営分析を一元管理する業務基幹システム。長期運用が前提。

# Purpose

「動くアプリ」ではなく、安全に運用でき、異常に気づけて、原因を追跡でき、
データを戻せる業務基幹システムにする。

# 本番環境

| | |
|---|---|
| Supabase | `fupfwpyvejzwrubomhov`（`itomedicalcare1995@gmail.com` の組織） |
| Vercel | チーム `naoto's projects` / プロジェクト `hp` |
| リポジトリ | `naoto07030113-design/HP`（**公開**。秘密情報を置かないこと） |

`unbfufnqajavptbsrsfc`（プロジェクト名「HP作成」）は**旧営業システム用の別プロジェクト**。
このシステムとは無関係なので触らないこと。

# CORE

| 層 | 技術 |
|---|---|
| Frontend | Next.js 14 App Router / TypeScript / Tailwind / Radix UI |
| Backend | `src/server/`（services / repositories / auth / permissions / logging） |
| API | `/api/v1/*`（`src/server/http/handler.ts` を必ず通す） |
| DB | Supabase (PostgreSQL) |
| Hosting | Vercel |

構造の詳細は `ARCHITECTURE.md`。

# CURRENT

**バックエンドへの段階的移行（予約領域まで完了）**

現在は「ブラウザから直接 Supabase」と「API 経由」が併存している。

| 領域 | 状態 |
|---|---|
| 予約（照会・キャンセル・変更・空き枠） | API 経由 |
| 患者（一覧・詳細・登録・更新・削除） | API 経由。院スコープ・ページング・論理削除・監査ログ対応 |
| カルテ（一覧・詳細・作成・更新・削除・改訂履歴） | API 経由。上記に加えて改訂履歴を保持 |
| 会計（一覧・作成・更新・入金・削除） | API 経由。**金額と伝票番号をサーバーで確定** |
| 予約・管理画面（カレンダー・一覧・当日受付） | API 経由。**枠の重複をサーバーで判定** |
| シフト（週次・月次・一括入力・前週コピー） | API 経由。表示中の期間だけを取得 |
| 日計表・ダッシュボード（経営/院別/スタッフ別） | API 経由。**集計をサーバーで実施** |
| 分析レポート・経営会議AI | API 経由。**集計と本文生成をサーバーで実施** |
| リマインド・CSV出力・患者詳細・物販予約 | API 経由 |
| 院・スタッフ・メニュー・お知らせ・休診日・物販商品・設定 | ブラウザから直接（公開情報のマスタ） |

**患者データの移行は完了**。ブラウザが直接読むのは公開情報のマスタだけになった。
残るのは `010_rls_clinic_scope.sql` の適用（DB 側でも遮断する）。

# DECISIONS

- **ディレクトリを frontend/ と backend/ に二分割しない。** Next.js の推奨構成から
  外れるため、`src/server/`（ブラウザに配信されない）に backend 相当を置く形にした。
- **認証は Bearer トークンで渡す。** セッションが localStorage にあり Cookie が
  飛ばないため。Cookie 方式（@supabase/ssr）への移行は影響範囲が大きく、後回し。
- **ロールは `app_metadata` のみを見る。** `user_metadata` は本人が書き換えられる。
- **未設定のロールは `receptionist`（最小権限）。** 以前は `admin` 扱いだった。
- **環境変数が無ければ起動を失敗させる。** 以前は本番へ暗黙にフォールバックしていた。
- **監査ログの書き込み失敗で業務を止めない。** ただし必ずエラーログには残す。
- **レート制限はプロセス内メモリ。** サーバーレスでは完全ではないが、無いよりよい。
  本格運用時に Redis へ差し替える。
- **患者向けの予約作成は `/api/intake` に一本化する。** 初診・再来で経路を分けず、
  枠の空きは必ずサーバーで再確認する。
- **スタッフへの LINE 通知はサーバー内部からのみ送る。** 通知用の公開エンドポイントは持たない。
- **自動テストは「壊れると業務が壊れる」判断だけに絞る。** 画面の見た目は対象にしない。

# COMPLETED

- 全画面の実操作による監査。院未選択で空白になる不具合、集計が¥0になる競合、
  保存失敗の無通知など、実バグを修正
- 患者向け予約サイトの演出（背景・遷移・完了時の演出・1分セルフケア）
- アーキテクチャとセキュリティの全体調査（P0 10件 / P1 11件を特定）
- **バックエンド基盤の新設** — エラー体系・構造化ログ・リクエストID・認証・認可・
  zod 検証・共通ハンドラ・レート制限・ヘルスチェック・middleware
- **P0 の情報漏洩を封鎖** — 未認証で全予約が読めた問題、他人の予約を書き換えられた問題を
  API 経由に置き換えて解消。通信を実測し、他の患者の情報が流れないことを確認済み
- Next.js を 14.2.35 へ更新（critical CVE の解消）
- 監査ログ・操作者記録・論理削除のマイグレーション作成（**未適用**）
- ドキュメント整備（ARCHITECTURE / SECURITY / RUNBOOK / BACKUP / PROJECT_STATE）
- **患者領域の API 化** — 一覧/詳細/登録/更新/削除を `/api/v1/patients/*` へ。
  院をまたいだ参照を拒否、受付の削除を拒否、検索とページングをサーバー側へ、
  削除を論理削除へ、閲覧・作成・更新・削除を監査ログへ記録。実機で確認済み
- **会計領域の API 化** — `/api/v1/invoices/*` へ。**金額の計算をサーバーへ移した**
  （以前はブラウザが計算した合計をそのまま保存しており、送信内容を書き換えれば
  任意の金額で会計を確定できた）。伝票番号の採番もサーバー側に移し、
  手元の件数から採番する方式をやめた。入金は不足額を拒否し、二重入金も防ぐ。
  合計1円を送りつけても正しい55,000円で保存されることを実機で確認済み
- **予約（管理画面）の API 化** — `/api/v1/reservations/*` へ。カレンダー・予約一覧・
  当日受付を移行。**枠の重複をサーバーで判定**し、同じ担当者の同じ時間帯に
  二重に入れられないようにした（同時操作でのダブルブッキングを防ぐ）。
  カレンダーは表示中の期間だけを取得する。削除は論理削除。
  カレンダーの `.catch(() => {})` による失敗の握りつぶしも解消
- **未認証で叩ける穴を塞いだ** — `/api/line/notify`（誰でも院の LINE 公式アカウントから
  一斉配信できた）を廃止し、通知は予約成立後にサーバー内部からのみ送るようにした。
  Web予約（`/api/intake`）は検証・レート制限・**枠が空いているかの再確認**を通すようにし、
  内部エラーをそのまま返していたのも直した。再来の予約もブラウザから直接
  DB に書いていたため、同じ経路へ寄せた
- **ログインをサーバー経由にした** — 失敗の連続をアカウント単位・端末単位で
  制限し、成功・失敗とも監査ログに残す。パスワードは受け取ってすぐ Supabase へ渡すだけで、
  ログにも監査ログにも出さない
- **共有端末の自動ログアウト** — 操作が止まってから既定30分でセッションを切る
  （1分前に予告）。離席中にカルテが見られる状態を残さない
- **保存失敗の握りつぶしを解消** — 設定・コミュニケーション・お知らせ・患者詳細・
  月次レポート・共有データ読み込みに残っていた `.catch(() => {})` をすべて潰した
- **患者詳細を API 経由に移した** — 患者・来院履歴・会計履歴をその患者の分だけ引く
- **自動テストと CI** — 会計計算 / 予約重複 / 権限と院スコープ / 入力スキーマ /
  ログ伏字 / レート制限 / 集計期間 の 76件。CI で lint・型チェック・テスト・ビルドを通す
- **患者データの移行を完了** — 分析レポート・経営会議AI・リマインド・CSV出力・
  患者詳細・物販予約を API 経由へ。`StoreHydrationProvider` が起動時に全患者・
  全カルテ・全会計をブラウザへ読み込んでいたのをやめ、共有ストアからも
  予約・シフト・物販予約を外した。**ブラウザが直接読むのは公開情報のマスタだけ**。
  実機の通信を計測し、患者データのテーブルに一度も触れていないことを確認済み
- **RLS に所属院の条件を入れるマイグレーションを作成（未適用）** —
  `010_rls_clinic_scope.sql`。患者データのテーブルはブラウザから 0 行に見えるようにし、
  マスタは参照のみ公開・変更はロールで制限する。判定用の関数
  （`auth_role` / `auth_clinic_id` / `can_access_clinic`）も同ファイルで定義する
- **日計表とダッシュボードの API 化** — `/api/v1/reports/daily` と
  `/api/v1/analytics/*` へ。**集計をサーバーへ移した**（以前はブラウザが
  全予約・全会計・全患者を読み込んでから絞り込んでおり、他院の売上と患者情報が
  端末に載るうえ、取得上限に達すると静かに欠けたまま「それらしい」数字が出ていた）。
  集計に使う行は必要な列だけをページで読み切るため、1000行の壁で欠けない。
  受付は日計・ダッシュボードを開けない。施術者は自院のみ。実機で確認済み
- **シフト領域の API 化** — `/api/v1/shifts/*` へ。表示中の期間だけを取得するようにし、
  シフトの全件取得をやめた。対象スタッフが本当にその院に所属しているかをサーバーで
  確認するため、他院のスタッフのシフトを自院として作ることができない。
  受付は参照のみ（編集は管理者と施術者）。一括入力・前週コピーは1リクエストにまとめ、
  途中で失敗して中途半端に入ることを防ぐ。保存失敗は画面に必ず表示する。
  一括入力ダイアログでスタッフが初期選択されず「一括適用」を押せなかった不具合も修正
- **カルテ領域の API 化** — `/api/v1/medical-records/*` へ。受付はカルテを読めない。
  **改訂履歴**を導入し、更新・削除の前に変更前の全内容を保存する（訂正しても
  前の記述が失われない）。履歴の閲覧は管理者のみ。詳細画面の保存失敗の
  握りつぶしも解消。実機で 2回の訂正が履歴に残ることを確認済み

# NEXT

1. ~~マイグレーションを適用する~~ **適用済み（2026-08-20）**
   - `008` / `009` / `010` を本番 Supabase (`fupfwpyvejzwrubomhov`) へ適用
   - 判定関数は `app_private` スキーマへ（`public` だと REST の RPC から直接呼べるため）
   - 旧セットアップが残していた「ログイン済みなら全操作可」ポリシーも撤去
   - 実測: 匿名・受付とも患者/予約/カルテ/会計/シフト/物販予約は 0 行、
     院・メニュー・スタッフは参照可、受付による院マスタ更新は 0 行で拒否
2. ~~管理者アカウントのロール設定~~ **完了（2026-08-20）**
   `naoto07030113@gmail.com` と `itomedicalcare1995@gmail.com` を `admin` に設定
3. **Vercel の Production 環境に環境変数3つを設定する**（デプロイ前に必須）
   - `NEXT_PUBLIC_CLINIC_SUPABASE_URL` = `https://fupfwpyvejzwrubomhov.supabase.co`
   - `NEXT_PUBLIC_CLINIC_SUPABASE_ANON_KEY` = `sb_publishable_jIbUWn3vDXHZtGF_DJ8a1A_kpMt-NcE`
   - `CLINIC_SERVICE_ROLE_KEY` = Supabase の secret key（Sensitive で登録）
4. 新コードを本番へデプロイする（現在の本番は5月のコード）
5. Supabase の「Leaked Password Protection」を有効化する
6. 論理削除をアプリ側に反映（現在は物理削除のまま）
7. 監視とアラート、ステージング環境

# OPEN ISSUES

| 優先 | 内容 |
|---|---|
| P0 | 新コードが本番へ未デプロイ（本番は5月のコード） |
| P0 | Vercel の Production 環境に `NEXT_PUBLIC_CLINIC_SUPABASE_URL` / `ANON_KEY` / `CLINIC_SERVICE_ROLE_KEY` が未設定。旧コードは URL とキーを直書きフォールバックしていたため動いていたが、新コードはフォールバックを廃止したので**このままデプロイすると本番が起動しない** |
| P1 | 論理削除は患者・カルテ・会計・予約に対応。シフトは予定であり業務記録ではないため物理削除のまま |
| P0 | バックアップの復元テストが未実施 |
| P1 | レート制限はプロセス内メモリのため、サーバーレスの複数インスタンス間で共有されない（本格運用では Redis 等へ差し替える） |
| P2 | lint の警告6件（`<img>` の使用3件、useEffect の依存漏れ2件、他）。挙動が変わりうるため未対応 |
| P2 | 巨大ファイルの責務分離（957行 / 669行 / 600行） |

# LAST UPDATED

2026-08-19
